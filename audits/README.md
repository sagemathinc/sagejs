# Compiler and capability audits

Files in this directory are deterministic, machine-generated baselines.  They
turn language and mathematical coverage into reviewable denominators: CI
rejects an unexplained regression, while intentional improvements produce a
small source-controlled diff.

`python-grammar.json` is generated by `pnpm python:grammar:audit --write`.
