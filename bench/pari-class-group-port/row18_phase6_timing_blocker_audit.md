# Row 18 Phase-6 timing source cut (closed)

Row 18 formerly connected the translated initial and retry mathematics through
dynamic Python. The Phase-6 resident root now closes that source cut with an
explicit 415-owner typed ABI, in-place transition, and one native entry call.

Run `node bench/pari-class-group-port/check_row18_phase6_timing_blocker.cjs`.
The executable readiness probe authenticates the fresh input, rejects changed
authority, pins the resident source generator and host, and proves that the
emitted root contains none of the former reflection/copy/process boundaries.
