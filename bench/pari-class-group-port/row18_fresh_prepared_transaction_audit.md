# Row 18 fresh-prepared transaction audit

## Boundary

The transaction accepts only the normalized prepared number-field object for
`3.1.1005907102200.3`. Its authenticated authority is
`2306e01429981dc6e956f10dd4c9528b157fbff76c1c137419e678a958de1dd0`.
The exact 22-key prepared schema is checked before authentication, so an added
retained relation, terminal owner, or answer is rejected rather than ignored.
No W0 file, retained intermediate owner, frozen class/unit answer, or reserve
artifact is a runtime input.

All mutable owners are newly allocated. In one Python invocation the pipeline
generates the factor base, collects relations, computes HNF and acceptance,
constructs the exact class witness, and reconstructs the unit. Those owners
remain private to the invocation. Only the neutral immutable result crosses
the boundary.

## Exact execution path

The translated resident graph generates a 41-ideal factor base and 47 initial
principal relations. Its first analytic attempt returns action `5`. The retry
reconstructs the selected prime descriptors from the same live Kummer catalog,
retains the live relation/log/HNF owners, and enters the connected collector.
It terminates with status `0`, 49 relations, kernel rank 8, class number 18,
invariant factor `[18]`, and a 192-bit packed regulator.

The older retained W0 path has 50 relations. That is not copied or reproduced
as an acceptance condition: the genuine fresh execution naturally accepts
after 49. The row-18 witness therefore validates the relation count and kernel
rank through all owner shapes and the identity
`kernelRank == relationCount - factorBaseSize`; it no longer treats 50 as part
of the mathematics. The existing 50-relation W0 regression still passes.

## Exact witnesses and publication

The source-derived presentation has determinant 18. The class witness selects
a factor-base ideal of exact order 18, rejects every proper divisor
`1, 2, 3, 6, 9`, constructs the corresponding principal generator, and checks
that its ideal HNF equals the eighteenth ideal power.

The rank-one unit reconstruction proves that the raw coefficients are a
relation dependency, performs exact factorback, obtains a unit of norm `+1`,
and verifies an exact integral inverse whose product is one. Its largest
coordinate has 39,377 bits.

The resulting immutable neutral envelope is 54,693 bytes with SHA-256
`5a409415b05fb76c07ee299dcdfd350336a8802da6540bac310cefc565c72e8e`.
Receipt authenticity is a module-local `WeakSet` property and does not survive
structural copying.

## Negative checks and scope

The focused checker verifies that:

- a structural receipt forgery is rejected;
- an injected retained terminal owner is rejected at the prepared boundary;
- inconsistent factor-base and kernel dimensions are rejected;
- the published envelope is immutable and its digest and neutral payload replay;
- no W0 path, timing field, reserve claim, or retained-owner claim is published.

This is correctness evidence for the internal PARI-correspondence tier. It is
not a qualified timing result and makes no public-completeness claim. PARI's
factor-base bounds and correspondence are recorded assumptions. The execution
also uses the separately reviewed mixed-cubic resident generalization that
admits signature `(1, 1)` and normalizes the analytic discriminant by absolute
value.
