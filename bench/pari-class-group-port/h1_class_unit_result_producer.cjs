"use strict";

// Reusable entry point for the genuine unified prepared-H1 root, its detached
// cold replay, and the final field-neutral publication.  The implementation
// remains in the focused checker so that its authentic differential and
// mutation coverage exercise exactly the same producer.

const {
  produceH1ClassUnitResult,
} = require("./check_h1_class_unit_result_adapter.cjs");

module.exports = { produceH1ClassUnitResult };
