# Final immutable internal completion composer

This source cut joins four already-live immutable authorities for the frozen
real cubic: class assembly, no-oracle unit/regulator completion, genuine
compact units, and exact torsion. The composer accepts no resident path,
fixture path, expected digest, PARI handle, or callback.

Publication requires the owners to agree on the resident and polynomial, the
trivial class quotient, the exact rank-two unit coordinates, the rigorous
full-rank regulator, compact-unit coordinate hash, and exact order-two
torsion. It also requires the live equal-bound honesty decision and six p2176
retry log records. Only after every check succeeds does a locked publisher
install the immutable envelope.

The resulting terminal distinction is intentional:

- `correspondence_complete = true`;
- `public = false` and `public_complete = false`; and
- unit saturation and the standard public proof payload remain missing.

The focused checker exercises concurrent idempotent publication and rejects
coordinated, rehashed mutations of honesty, retry precision, resident
identity, compact provenance, compact/unit linkage, and the final public flag.
