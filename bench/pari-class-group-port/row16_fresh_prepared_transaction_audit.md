# Row 16 genuine fresh-prepared transaction

This transaction starts from the authenticated normalized prepared maximal
order for development-panel row 16.  In one private Python invocation it
derives PARI 2.17.4's factor base, 54 principal relations over 48 ideals,
relation HNF, `C3 x C3 x C3` Smith presentation, three exact ideal-cube
witnesses, the primitive rank-one unit and its exact inverse.  It publishes an
immutable field-neutral correspondence result with class number 27, one exact
unit, torsion order two and the accepted packed regulator.

The runtime request contains no W0 event, retained factor owner, relation/HNF
owner, accepted result, unit, timing datum or reserve field.  Mutable owners
are newly allocated from the reviewed resident capacity manifest.  The
transaction brands its receipt in a module-private `WeakSet`; copying either
the receipt or its enumerable data loses authenticity.

The formerly totally-real resident cubic entry now admits signatures `(3,0)`
and `(1,1)`.  Its discriminant-sign guard requires positive polynomial
discriminant exactly for three real places.  The analytic inverse-hR call uses
the absolute field discriminant, as required by regulator normalization.  The
existing row-1 checker remains the real-cubic regression; this row is the
mixed-cubic acceptance regression.

This is development-only, upstream-assumed PARI correspondence.  It does not
claim qualified timing, reserve access, unconditional factor-base bounds, a
rigorous regulator enclosure or public Sage.js completion.
