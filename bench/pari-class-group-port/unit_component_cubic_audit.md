# Real-cubic unit component handoff

This lane connects the successful high-precision real-cubic `getfu` retry to
the fixed `UnitComponentOutput` accepted by `class_group_final_state.py`.

The publication boundary validates both reconstructed algebraic units by
forming their exact multiplication matrices, checking determinant `+/-1`, and
replaying an exact inverse.  It also requires the composed two-row exponent
matrix over all 73 accepted relations and the successful resident-to-retry
link state.  Those values cannot be added to the final-state evidence object,
whose exact-key schema is already fixed, but rejection happens before any
component is returned.

The final evidence authenticates four seven-word packed-log entries in the
accepted candidate, retains the accepted regulator triplet, and derives an
exact dyadic rank-two determinant from the p2176 retry logarithms.  The source
oracle is pristine PARI 2.17.4 with instrumentation limited to the static
`getfu` boundary.  The focused bridge checker separately proves that the
retry logs, factor transform, units, norms, and composed provenance agree on
CPython, JavaScript integers, GMP buffers, and tagged buffers.

Scope remains narrow: one totally real cubic, rank two, with the authentic
p2240 embedding retry.  This is a compatible unit-component producer, not a
claim that the class-group final driver is connected in this lane.  Mixed
signature reconstruction and general wide unit lattices remain elsewhere.
