# Row 0 Phase 6 matched resident boundary

`row0_phase6_matched_kernel_host.cjs` authenticates the frozen normalized
prepared NF, compiles the unified H1 root, and allocates pristine bounded native
owners before the clock. `runInvocation` clocks exactly one native call and
inspects its class/unit output only after the clock stops. No subprocess,
filesystem access, replay, hashing or publication is in that interval.

`row0_phase6_pari_prepared_adapter.c` prepares `nfinit` before `READY`, restores
the prepared stack for every sample, and clocks exactly pristine PARI 2.17.4
`bnfinit0(nf, 0)`. Its JavaScript wrapper authenticates the pinned archive,
`buch2.c`, and shared library.

The shared fail-closed projection contains the field/polynomial, class number
and invariant factors, unit rank, regulator presence, torsion order, log shape,
and flag-zero completion mode. The focused checker also reruns the existing
untimed fresh correctness transaction unless
`--skip-fresh-correctness` is supplied for a short boundary diagnostic.

Single observations are explicitly unqualified: no ratio is published until a
quiet-host alternating campaign is admitted by the shared Phase 6 coordinator.

```sh
prlimit --as=4294967296 --cpu=600 -- \
  node bench/pari-class-group-port/row0_phase6_matched_kernel_check.cjs
```
