# Row 6 prepared-field class-and-unit transaction

This transaction starts from the authenticated prepared-number-field projection,
immutable factor-base owner, and immutable accepted Gate-C owner. It computes the
post-1137 analytic/Smith result, exact source-schedule ancestry, cubic class
witnesses, compact rank-two unit result, and C7 envelope. Previously published
post-terminal answers and the frozen W0 transcript are excluded as runtime
inputs; W0 is opened only after the final envelope is immutable.

Each heavy stage runs in a separate process under a 4 GiB address-space/RSS and
600 second CPU limit. Intermediate files live in a private temporary directory;
only the final read-only envelope is atomically published, and failure removes
the private transaction state. This isolation prevents compiled modules and HNF
workspaces from accumulating across stages.

The qualifying run completed in 84.370 seconds with 963,064 KiB maximum stage
RSS. Its 6,746,371-byte envelope has SHA-256
`b3bfd9122875729853f0421ab5ffe6220f161f07add30a9753eb79df784dad73`
and mathematical authority
`713b8d9e2b73b870faba7db43a8f618d283264b1c20b84c7d7703fa71decb2a8`.
It proves class group `C2 x C2`, class number 4, retains the exact compact
rank-two unit transform, and faithfully records `not_given(LARGE)`. Twelve
component/publication mutations are rejected.

This is correspondence-complete but public-incomplete. The transaction admits
the retained factor-base and Gate-C owners, rather than rebuilding relation
collection from the prepared field, and it does not provide an independent
unit-saturation proof or expanded fundamental units.
