# Live prepared `h = 1` worker

This lane exercises the executable composition edge for the qualified real cubic,
but deliberately does **not** publish Outcome C.
The worker receives only the sanitized prepared-number-field owners, invokes
the resident generated candidate once, and passes its mutable integer owners
directly to `pari_live_h1_owner_bridge`.  It then publishes the trivial class
group, equal-bound honesty decision, compact rank-two unit provenance, and
exact real-cubic torsion in a specialized experimental envelope.

Kernel compilation and loading occur before the exclusive timer. The candidate
and bridge are compiled as one ordinary-Python graph because native buffer
ownership is addon-local; this also makes the in-memory ownership edge
explicit rather than relying on an unsupported cross-addon transfer. Inside
`runPreparedH1` there are no filesystem reads, JSON intermediates, subprocesses,
PARI calls, fixtures, or expected-answer bits.  Each repetition allocates fresh
owners. The root reconstructs both exact units from the live principal
relations, independently rebuilds the ordered embedding and six logarithms at
p2176, and recomputes the regulator determinant. The p2176 determinant and
resident p192 path differ by four terminal p192 ulps; the qualified corridor
accepts at most four, records the actual delta, and rejects a five-ulp sibling.
The precision, 73 relation count, 15 active columns, seven kernel columns, and
four-ulp corridor are frozen diagnostic controls. They are not live authorities.
Consequently `correspondenceComplete` remains false and the terminal state names
the missing live logical-length, retry-policy, and rigorous-enclosure inputs.
This is pinned experimental evidence, not Outcome C or a general regulator
certificate.

The focused checker runs two fresh adapter calls, verifies their deterministic
result and work digests, and requires the incomplete specialized status.
