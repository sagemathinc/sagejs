# Row 3 fresh-prepared transaction gap audit

Status: the prepared-only factor-base, complete relation, HNF, analytic
acceptance, Smith, compact unit/getfu, class witness, and neutral
correspondence transaction is executable.  It is internally correspondence
complete, but makes no public-completion or timing claim.

`row3_fresh_prepared_transaction.cjs` packages that graph behind the standard
`runFreshPrepared(prepared, outputDirectory)` boundary.  Its receipt has a
module-local `WeakSet` brand, a non-enumerable replay-verified result, explicit
fresh/runtime-input fields, and a read-only content-addressed durable envelope.
The focused transaction checker rejects forged receipts, injected prepared
fields, and prepared-field mutation before completing a genuine run.

## Requested boundary

The intended transaction starts with only the authenticated normalized
prepared-number-field projection for development-panel row 3 and, in one
private invocation, derives the factor base, 675 retained relations, HNF,
class witness, compact rank-two units, and neutral correspondence result.  It
must not open the frozen default-driver trace (`W0`) or a retained derivative
of that trace.

## Existing downstream arithmetic

The committed suffix is substantial:

- `row3_real_cubic_class_witness.py` derives and replays the compact order-six
  witness once it receives a complete presentation owner.  It does not itself
  open `W0`.
- `row3_rank2_unit_authority.py` contains the rank-two lattice/getfu, exact
  relation-provenance, norm, and sign arithmetic.
- `row3_c7_result_composer.cjs` constructs and verifies the neutral final
  envelope from presentation, class, and unit owners.

These pieces explain why a fresh connector is worthwhile, but they do not yet
form one.

## Exact blocking dependency

There is no complete row-3 prepared-only producer for the
factor/relation/HNF prefix.
The only row-3 presentation producer is
`row34_real_cubic_presentation_coordinator.cjs`, whose command-line contract
requires `--pristine-w0` and its pinned digest.  Its Python producer obtains
all of the following from `W0.events`:

- the 675 relation records and principal generators from the retained HNF
  event;
- the factor-base permutation and event stream used to reconstruct the 668
  factor ideals;
- the accepted unit lattice and packed regulator;
- the terminal result used for detached comparison.

Consequently that owner is a correct replay owner, not a fresh-prepared
execution owner.

The unit suffix has a second, independent live-input obstruction.
`compose_row3_rank2_unit_authority` requires the pinned pristine `W0`, reruns
the HNF/log schedule from its retained HNF event, and honestly publishes
`sourceLogs.frozenW0UsedAsInput=true` and
`sourceLogs.preparedNfLiveRoot=false`.  The current C7 validator requires those
same values.  Wrapping these producers in one process would therefore not
change the input boundary.

`row3_prepared_initial_base_frontier.cjs` now closes the first part of that
gap.  From authenticated prepared data and the neutral exhaustive runtime
prime table, it executes the translated degree catalog, PARI initial-base
policy, requested Kummer descriptor catalog, selected prime-ideal packet
construction, subfactor policy, and rational-relation initialization.  It
independently publishes `C1=C2=5301`, `KC=KC2=668`, `KCZ=KCZ2=446`, all 668
descriptors and ideal HNFs, the exact 668-entry permutation, the four-entry
subfactor, and the 116 initial relations toward target 675.  Its checker opens
the retained factor-base and initialized events only after this owner exists
and obtains exact differential agreement for all of those values.

`row3_prepared_relation_hnf_frontier.cjs` closes the remaining presentation
prefix.  It consumes only the authenticated prepared bundle and live initial
frontier, then executes the translated collector from 116 through 675
relations, retains the exact 450,900 relation cells, 675 principal generators
and packed logarithms, runs HNF, computes the analytic inverse hR from the
prepared discriminant and complete splitting-degree catalog, and runs the
field-neutral acceptance and Smith tail.  Its detached checker opens `W0`
only after publication and exactly matches every retained relation,
generator, logarithm, `W`, `B`, `C`, permutation, accepted regulator,
rank-two relation lattice, class number 6, and invariant `[6]`.

`row3_fresh_unit_suffix.py` supplies a private fresh-owner entry point without
weakening the historical W0-backed unit authority.  It replays the source HNF
transform from live relations/logs, runs the existing regulator bridge and
`getfu(LARGE)` arithmetic, proves both compact units have trivial principal
ideals, replays all generator norms and real signs, and invokes the existing
exact order-six class witness against a privately constructed live
presentation owner.  The JavaScript transaction then seals those outputs in
the field-neutral class-and-unit correspondence envelope with explicit
prepared-root and conditional-PARI assumptions.

## Narrow implementation needed next

The exploratory transaction now has the requested fresh branch.  Integration
work should split its private owner into reviewed reusable APIs, add a detached
publication authority if public publication is desired, and replace the
temporary CPython suffix boundary with the corresponding native/private call
graph.  The current historical pinned replay entry points remain unchanged.

## Conclusion

The fresh prepared transaction now reaches an internally sealed class-and-unit
correspondence without replaying the retained 22.9 MiB trace.  There is no
remaining mathematical dependency in this bounded experiment.  Remaining
work is productization: native suffix integration, reviewed owner boundaries,
detached publication authority, representative timing, and wider fields.
