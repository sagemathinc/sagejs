# Row-8 fresh-prepared sentinel audit

This sentinel begins with exactly one authenticated normalized prepared-number-field
envelope. The root factor base, initial relations, both retry relations, all three
HNF checkpoints, the analytic inverse-`hR`, regulator acceptance, and neutral
result envelope are created during one invocation in private owners.

The transaction does not open or name W0, a capsule, a retained accepted owner,
or any `/scratch` artifact. The test harness may normalize the historical prepared
bundle before the transaction and may compare results after publication; neither
operation supplies an answer to the mathematical call graph.

Publication requires two independent unforgeable brands: the transaction-local
receipt `WeakSet` and the neutral result module's detached-authority `WeakSet`.
Copying a receipt, injecting an accepted answer beside the prepared envelope, or
mutating a detached payload is rejected or cannot change the published result.

The result deliberately makes no timing, allocation-reserve, proof, or public
completion claim. Its GRH, bound, factor-base, and PARI-correspondence statements
remain explicit assumptions. Exact fundamental units are `not_given(PRECI)`;
the class group, unit rank, torsion, and regulator enclosure are published at the
internal correspondence tier.
