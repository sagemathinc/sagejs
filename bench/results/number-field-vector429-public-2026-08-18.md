# Vector429 public maximal-order receipt

Three fresh `K.maximal_order(trace=True)` processes completed in 4.731130,
4.780264, and 4.730427 seconds. The exact median was **4.731130 seconds**;
all three samples were below the 5-second public target. Peak process-group
RSS ranged from 739,692,544 to 763,424,768 bytes.

Every sample returned the same authenticated basis projection SHA-256,
`8cd3e728367becb8f7c511e8bcdf1137439fa27ff5bb876df0a001a6c4e7328c`.
The exact equation-order index and field discriminant matched the frozen
vector429 fixture, and the public certificate reported `certified=true`.
The trace recorded a carried Round-2 proof for the native local batch, a
current-call-attested OM proof at p=7, and an
`authenticated-local-portfolio` global certificate.

The critical scheduling change lets the private, allowlisted OM worker accept
its queued task while its isolated evaluator is still initializing. Ordinary
`multiprocessing.Pool` creation remains synchronous. Worker source and module
identity are checked before selection, and the parent accepts only an attested
result bound to the exact job. This overlaps worker startup and OM arithmetic
with discriminant decomposition, composite work, and the carried native batch.

The BL proof path independently reconstructs the exact generator lattice with
modular HNF, then checks the remaining generator-square membership through a
bounded triangular solve. The general copied-result checker separately proves
equation-order containment and uses a source-transparent exact fallback on
capacity failure. Focused compiled/dynamic and corruption differentials pass.

As the required suppressed-alternative control, forced p=7 Round 2 did not
complete within its 15-second hard bound. The process group was terminated and
left no descendant. The raw timing, stage, identity, and runtime details are in
the adjacent JSON receipt.

After the final FFI inventory, production-kernel publication, and portable
32-bit-GMP runtime fixes, three additional fresh samples completed in
4.733276, 4.718116, and 4.686681 seconds (median **4.718116 seconds**). All
three again returned the same authenticated projection and stayed below the
target, confirming the receipt against the final validated source and
artifact identities recorded in JSON.
