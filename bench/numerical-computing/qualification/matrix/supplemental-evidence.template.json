{
  "schema": "sagejs.numerical-qualification-supplemental-template/v1",
  "id": "sagejs-numerical-product-supplemental-evidence",
  "description": "Fail-closed component and operating-system evidence that cannot be honestly derived from a product adapter observation alone.",
  "requirements": [
    {
      "id": "cminpack-native-sanitizers",
      "platform": "linux-x64",
      "status": "pending",
      "required_evidence": ["address", "undefined", "leak"],
      "acceptance": "The exact selected cminpack native C component executes successful lmdif/lmder, callback-stop, and repeated lifecycle workloads under ASAN, UBSAN, and LSAN with no finding; the same candidate separately passes the Wasm adapter corruption, cancellation, callback, allocation-failure, and recovery gates. Neither half substitutes for the other."
    },
    {
      "id": "nlopt-native-sanitizers",
      "platform": "linux-x64",
      "status": "pending",
      "required_evidence": ["address", "undefined", "leak"],
      "acceptance": "The exact selected NLopt native C component executes every algorithm retained in the final Wasm artifact, forced-stop, and repeated lifecycle workloads under ASAN, UBSAN, and LSAN with no finding; the same candidate separately passes the Wasm adapter corruption, cancellation, callback, allocation-failure, and recovery gates. COBYLA is not eligible while its upstream f2c indexing triggers object-size UBSAN (NLopt issue 611)."
    },
    {
      "id": "numerical-wasm-destructive-faults",
      "platform": "linux-x64",
      "status": "pending",
      "required_evidence": ["allocation-failure", "corrupt-region", "artifact-digest-mismatch", "post-failure-recovery"],
      "acceptance": "Authenticated cminpack and NLopt reactors fail closed on every injected fault, release all contexts/handles/allocations/bytes, expose no private callback or resource details, and remain reusable after recovery."
    },
    {
      "id": "browser-process-tree-memory",
      "platform": "linux-x64",
      "status": "pending",
      "required_evidence": ["chromium", "firefox", "webkit", "worker-replacement"],
      "acceptance": "For Chromium, Firefox, WebKit, and the Chromium worker-recovery row, the qualification collector authenticates operating-system process-tree peak memory for the exact browser executable. The dedicated baseline/64-MiB touched-worker workload must increase peak RSS by at least 32 MiB; browser-heap counters are supplemental and cannot substitute for process-tree evidence."
    }
  ]
}
