diff --git a/asm.h b/asm.h
--- a/asm.h
+++ b/asm.h
@@ -11,6 +11,10 @@
 extern "C" {
 #endif
 
+#if defined(SAGEJS_FFPOLY_PORTABLE) || !defined(__x86_64__)
+#include "sagejs_ffpoly_word.h"
+#else
+
 #define _asm_div_q_q(q,r,x,y)				asm ("divq %4" :"=a"(q) ,"=d"(r) : "0"(x), "1"(r), "rm"(y))
 #define _asm_mult_1_1(z1,z0,x0,y0)		asm ("mulq %3" :"=a"(z0) ,"=d"(z1) : "a"(x0), "rm"(y0))
 #define _asm_mult_2_2_1(z1,z0,x1,x0,y0)	asm ("mulq %3" :"=a"(z0) ,"=d"(z1) : "a"(x0), "rm"(y0));(z1)+=(y0)*(x1)
@@ -73,6 +77,8 @@ static inline void _asm_square_3_2 (unsigned long z[3], unsigned long x[2])
 	z[2] = x[1]*x[1]+R1;
 }
 
+#endif
+
 #ifdef __cplusplus
 }
 #endif
diff --git a/cstd.h b/cstd.h
--- a/cstd.h
+++ b/cstd.h
@@ -9,12 +9,17 @@ extern "C" {
 	See LICENSE file for license details.
 */
 
+#include "sagejs_ffpoly_word.h"
 #include <stdint.h>
 #include <stdlib.h>
 #include <stdio.h>
 #include <time.h>
 #include <limits.h>
+#if defined(_WIN32)
+#include <process.h>
+#else
 #include <unistd.h>
+#endif
 #include <memory.h>
 #include <math.h>
 
@@ -64,6 +69,14 @@ static inline unsigned long cstd_seed(void)
 {
 	if ( ! _cstd_seed ) {
+#if defined(_WIN32)
+		_cstd_seed = ((uint64_t)_getpid() << 32) ^ (uint64_t)time(0);
+#elif defined(__wasi__)
+		/* WASI has no stable host or process identity.  Use a deterministic,
+		   nonzero seed and avoid importing an emulated getpid. */
+		_cstd_seed = UINT64_C(0x534147454a534646);
+#else
 		_cstd_seed = (((unsigned long)gethostid())<<32) + getpid();		// make sure seed is different in different processes/hosts, even if seeded at (approximately) the same time
+		_cstd_seed *= time(0);
+#endif
-		_cstd_seed *= time(0);
 	}
 	return _cstd_seed;
@@ -76,11 +89,11 @@ static inline unsigned long cstd_seed(void)
 #define _min(a,b)		((a)<(b)?(a):(b))		// generic min, not safe with side-effects
 
 // Some handy utility functions
-static inline unsigned long _asm_highbit (unsigned long x) { asm ("bsrq %0, %0" : "=r" (x) : "0" (x)); return x; }
-static inline unsigned long _asm_lowbit (unsigned long x) { asm ("bsfq %0, %0" : "=r" (x) : "0" (x)); return x; }
+static inline unsigned long _asm_highbit (uint64_t x) { return sagejs_ffpoly_highbit64(x); }
+static inline unsigned long _asm_lowbit (uint64_t x) { return sagejs_ffpoly_lowbit64(x); }
 static inline unsigned long ui_ceil_ratio (unsigned long a, unsigned long b)
 	{ register unsigned long x = a/b;  return ( x*b<a ? x+1 : x ); }
-static inline int ui_len (unsigned long x) { return LONG_BITS-__builtin_clzl(x); }
+static inline int ui_len (unsigned long x) { return 1+sagejs_ffpoly_highbit64(x); }
 static inline unsigned long ui_min (unsigned long a, unsigned long b)
 	{ return ( a < b ? a : b ); }
 static inline unsigned long ui_max (unsigned long a, unsigned long b)
