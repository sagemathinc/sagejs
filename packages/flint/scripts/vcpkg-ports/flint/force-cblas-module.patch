diff --git a/CMakeLists.txt b/CMakeLists.txt
index a802d2502..639765478 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -118,8 +118,14 @@ if (WITH_NTL)
 endif()
 find_package(Python REQUIRED)
 
 find_package(CBLAS)
-set(FLINT_USES_BLAS ${CBLAS_FOUND})
+# Config packages, including vcpkg's CBLAS wrapper, may provide the canonical
+# imported target without also defining the legacy CBLAS_FOUND variable.
+if(TARGET CBLAS::CBLAS)
+    set(FLINT_USES_BLAS ON)
+else()
+    set(FLINT_USES_BLAS ${CBLAS_FOUND})
+endif()
 
 if(CMAKE_BUILD_TYPE STREQUAL Debug)
   set(FLINT_WANT_ASSERT ON)
