{
  "name": "@sagemath/sagejs-graph",
  "version": "0.0.1",
  "description": "Optional native graph algorithms for Sage.js",
  "license": "GPL-3.0-only",
  "private": true,
  "type": "commonjs",
  "main": "index.cjs",
  "engines": {
    "node": ">=22.22.2"
  },
  "files": [
    "build/Release/sagejs_graph.node",
    "build/generated-ffi/sagejs_igraph_ffi.node",
    "build/generated-ffi/manifest.json",
    "generated/ffi_host.py",
    "include",
    "index.cjs",
    "README.md"
  ],
  "scripts": {
    "install": "node scripts/install.cjs",
    "build:deps": "node scripts/build-deps.cjs",
    "build:addon": "node-gyp rebuild",
    "build:ffi": "node ../../scripts/build-ffi-host-adapter.cjs igraph",
    "build": "pnpm run build:deps && pnpm run build:addon && pnpm run build:ffi",
    "test": "node --test --test-reporter=spec test/build-deps.cjs test/test.cjs"
  },
  "devDependencies": {
    "node-gyp": "^13.0.1"
  }
}
