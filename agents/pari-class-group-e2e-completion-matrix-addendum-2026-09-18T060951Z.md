# PARI class-group end-to-end completion-matrix addendum

Audit timestamp: `2026-09-18T06:09:51Z`  
Audited commit: `0b0b849c102ca25e050e62d730c435c66525eb26`  
Baseline matrix: `agents/pari-class-group-e2e-completion-matrix.md` at
`d0ffd32bba0bbed4c3ea3fc4b9f7e96cd59256b0`  
Plan: `agents/pari-class-group-end-to-end-native-plan.md`

This is a strict current-HEAD addendum, not a replacement for the baseline
matrix. It audits committed evidence only. No focused or expensive checker was
rerun for this addendum. Statuses below distinguish a translated dependency
cut, a retained-owner replay, a connected prepared-input computation, and a
qualified timing result.

The result remains **not complete** and the formal campaign outcome remains
**D (inconclusive)**. Current committed evidence beyond what the baseline
matrix recorded closes one required honesty fixture, materially advances all
three missing development rows, adds a second executable compact-tier row, and
removes a row-14 continuation-allocation boundary. It does not add a new
terminal development row, a qualified timing pair, or a prepared-input adapter
for the frozen population.

## Current-HEAD corrections and deltas

1. **Row 19 now has a genuine prepared prefix.** Starting only from the
   authenticated prepared `nfinit` projection, it derives all five index-prime
   decompositions, 424 selected factor-base ideals, the subfactor choice, and
   71 initial exact relations. It still lacks the `71 -> 423 -> 430`
   collector/HNF continuation, rank-nine class result, rank-one unit suffix,
   and C7 publication.
2. **Row 23's prime-131 dependency is closed.** The prepared maximal-order
   graph computes all three descriptors and exact packet HNFs above 131. The
   baseline matrix's wording that prime 131 itself was still unsupported is
   stale. Complete factor-base production, 40 relations/HNF, the order-six
   witness, rank-four units, and C7 remain missing.
3. **Row 21 has a connected prepared factor base and a translated rank-three
   suffix, but not a connected result.** All 24 factor-base ideals are derived
   from prepared state. The unit-lattice probe translates integer/real LLL,
   `cleanarchunit`, `fixarch`, and private `getfu` factor selection, but still
   consumes frozen HNF/log/lattice inputs and explicitly publishes no C5/C6
   owner. The 32-relation/HNF/log owner and live three-RHS `getfu` unit
   reconstruction remain missing.
4. **The selected successful honesty fixture is computed honestly.** Six
   degree-five collector observations are computed from prepared owners and
   consumed by the translated unequal-bound scheduler without a PARI status
   vector. This meets the plan's predeclared immediate-success honesty branch.
   It does not establish general automorphism-orbit or failure/retry honesty.
   The frozen Phase-1 ladder JSON/checker still labels
   `successful-full-honesty` missing and must be updated before the ladder
   contract itself is closed.
5. **Row 6 now executes compact flag-one replay.** It recomposes 2,274 compact
   coefficients and proves 2,260 exact factor-base products vanish without
   expanding units. It remains a retained-C7-owner diagnostic: no fresh PARI
   flag-one call, no matched timing, and no qualification switch is enabled.
6. **Row 14 now reuses Gate-C continuation storage.** One 14,146,540-byte
   typed envelope replaces 174 of 215 owner constructions and all 96
   continuation scratch zero arrays. A capped authentic profile preserves the
   result and measures Gate C at 28.143 s with 914,200 KiB peak RSS. Initial
   collector/`hnfspec` fixed-owner materialization and native arithmetic remain
   dominant, and this is not an alternating ratio.
7. **Timing-host preflight selected `opt` only prospectively.** Neither `opt`
   nor `bench-1` currently satisfies the Phase-6 contract. `opt` still needs a
   clean frozen candidate, writable `/scratch`, real exclusivity, and an
   accepted fixed-frequency authority. No campaign was staged or run.

## Requirement-by-requirement status

### Objective and boundary

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| Ordinary, attributed, CPython-parseable translated Python executes the connected PARI class-and-unit algorithm. | **Partial** | The three missing rows gained translated or connected prefixes, but none gained a complete prepared-to-result root. |
| Timed input is prepared maximal-order `nfinit` state only. | **Proven for H1 and row 14; not general** | Row 19's new prefix and row 21's factor base also honor the prepared boundary, but they stop before terminal computation. Retained-owner rows remain ineligible. |
| End-to-end output contains every required class, transform, ideal, unit, regulator, replay, and assumption field. | **Proven on selected fields; not panel-wide** | No new terminal output was added. Rows 19, 21, and 23 remain incomplete. |
| Publication is atomic/idempotent and failures cannot publish partial results. | **Proven on selected roots and the selected honesty transaction** | Honesty mutation/rejection is transactional; missing rows still have no final publication to validate. |
| No PARI class/unit routine or precomputed answer is behind a claimed prepared boundary. | **Proven for the two complete strict roots and the new narrow prepared cuts; not general** | The row-21 lattice suffix deliberately uses frozen W0-derived inputs and is nonpublishable; it is not counted as a prepared result. |
| Flag-zero and compact flag-one tiers are distinct and separately measured. | **Flag-zero partial; compact replay 1/12, timing 0/12** | Row 6 adds the first retained-owner compact replay beyond row 20's admission/authority seed. Neither is a fresh, timed, matched flag-one campaign; manifest execution switches remain false. |
| Internal correspondence and public certification remain distinct. | **Proven distinction; public certification absent** | No new evidence supplies unit saturation/index-one or the public proof payload. |

### Phase 0 — integration spine

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| Required branch ancestry and merge order. | **Proven at the Phase-0 checkpoint** | Unchanged. |
| Regenerated optimizer artifacts. | **Proven at the Phase-0 checkpoint** | Unchanged. |
| Only the two authorized HNF changes imported. | **Proven by patch identity** | Unchanged. |
| Architecture, strict Python, changed-file, compiler, and native gates. | **Proven for frozen Phase 0 only** | No current-HEAD full-suite receipt was created by the post-baseline work. |
| Reproduce 7,081 outputs, malformed controls, prepared roots, and required backends. | **Proven for frozen Phase 0 only** | Unchanged. |
| Freeze commit/toolchain/code/resource/output identities. | **Proven for Phase 0; current accounting incomplete** | Current code/resource state still needs a final freeze. |
| Private pristine PARI 2.17.4 on every oracle/timing host. | **Development/oracle host proven; prospective `opt` candidate found; final-host gate open** | `opt` has matching archive/source and a pristine private build, but no final timing-host qualification exists. |

### Phase 1 — observability and ladder

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| Freeze 4 sentinels, 12 additional fields, and 8 reserves. | **Proven** | Unchanged 24-field panel; reserves remain closed. |
| Trace all 16 development fields under pristine PARI 2.17.4. | **Proven** | Unchanged. |
| Include target degrees and at least four fresh >1 s PARI costs. | **Identity proven; fresh-cost gate missing** | No 24-field fresh timing receipt. |
| Keep failures/timeouts in denominator and reserves closed until freeze. | **Infrastructure proven; execution absent** | Neutral execution machinery exists, but the sealed runner remains disabled. |
| Predeclare and close missing branch fixtures. | **Partial** | The selected successful honesty implementation is now real, but the frozen ladder manifest/checker still says it is missing; `precision-escalation` remains missing in that contract. |
| Required bounded traces for changed stages; tracing disabled in timing. | **Partial** | New cuts have focused hashes/traces, but no consolidated vocabulary covers every general branch or missing terminal row. |

### Phase 2 — relation collection and HNF retries

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| Faithful `small_norm`/`rnd_rel`/cache/subfactor/permutation/RNG schedule. | **Partial, field-specific** | Row 19 reaches the authentic initial 71 relations; it does not run the main collector continuation. |
| Empty/nonempty `W`, deficiencies, repeated append, random fallback, rebuild, factor-base enlargement, and growth. | **Partial** | Successful immediate honesty is closed; generic enlargement, natural random fallback, and general retry remain open. |
| Allocate bounded owners once, use prefixes, resume, and fail safely on capacity miss. | **Partial, materially improved for row 14 Gate C** | Row 14 reuses continuation owners, but initial collector/`hnfspec` owner construction and a general resident state machine remain. |
| Four sentinels plus fixtures agree with PARI without callbacks/answer fixtures. | **Not met** | The selected honesty fixture now qualifies, but there is still no consolidated four-sentinel gate and the ladder precision fixture remains open. |

### Phase 3 — exact sparse/HNF/SNF envelope

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| Resident presentation, cleanup, rank, append, logs, and exact owners. | **Partial** | Row 19 adds only its prepared prefix; rows 21/23 still lack live relation/HNF owners. |
| Full Smith form and independent `U W V = D` replay. | **Proven on completed field-specific chains; not general** | No new terminal transform chain. |
| `ZM_inv`, rounded divisions, alias contracts, principal provenance. | **Partial** | Unchanged at the panel level. |
| All 12 additional development fields complete the envelope. | **Not met: 9/12 terminal** | Rows 19, 21, and 23 remain the exact missing rows despite their new dependency cuts. |

### Phase 4 — units and precision

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| Source-identical lattice extraction, both LLLs, cleanup, regulator, and precision decisions. | **Partial, rank-three translation added** | Row 21 closes the rank-three lattice/cleanup dependency only; its inputs are frozen and its live reconstruction is absent. Row 23 rank four remains missing. |
| `getfu`, exact unit/inverse/norm/principality, matched `not_given`. | **Partial** | Row 21 adds private factor selection, not its three solves or exact unit derivation. |
| Large-unit case proves no accidental eager expansion. | **Proven for correspondence cuts and row-6 compact replay** | Row 6 strengthens this with a large compact coefficient and explicit eager-owner rejection; no matched compact timing follows. |
| Sentinels plus 12 development fields produce required unit/regulator decisions. | **Not met** | Terminal count remains 13/16 overall and 9/12 additional fields. |

### Phase 5 — honesty, generators, final state

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| Successful conditional resident `be_honest` on a predeclared unequal-bound case. | **Met for the selected immediate-success fixture** | Six fresh degree-five probes, three transient `KCZ` increments, and exact restoration run without answer statuses. General automorphism and failure/retry paths are not thereby proven. |
| `class_group_gen`, full transforms, generators, witnesses, logs, and final class state. | **Proven on selected roots; not general** | No new missing row reaches this suffix. |
| `cleanarch` retry and immutable final assembly. | **Partial** | No new row gains final assembly. |
| One immutable internal result/replay verifier with mutations of every material family. | **Partial, strong field-specific evidence** | New dependency probes add controls but do not create a panel-wide verifier. |
| Final gate: all outputs/witnesses/replay, atomic construction, no hidden answers. | **Met only for individual strict fields** | H1 and row 14 remain the strongest prepared-input instances. |

### Development-field matrix

| Row | Terminal evidence | Prepared connected progress at current HEAD | Still missing |
| ---: | --- | --- | --- |
| 0 | yes | complete strict prepared root | qualification and compact tier |
| 1 | yes | retained-owner chain | prepared-only root |
| 3 | yes | retained/W0 chain | prepared-only root |
| 4 | yes | retained/W0 chain | prepared-only root |
| 6 | yes | prepared field plus retained factor-base/Gate-C owners; new compact replay | prepared-only root and matched compact run |
| 8 | yes | retained/W0 chain | prepared-only root |
| 10 | yes | retained large-owner chain | prepared-only root |
| 11 | yes | retained/W0 chain | prepared-only root |
| 13 | yes | prepared field plus retained prepared factor-base/initial relations | prepared-only factor-base/relation derivation |
| 14 | yes | complete strict prepared root; reusable Gate-C continuation storage | approved-host alternating qualification |
| 16 | yes | retained mixed-cubic chain | prepared-only root |
| 18 | yes | retained mixed-cubic chain | prepared-only root |
| 19 | **no** | prepared factor base, 424 packets, and 71 initial relations | collector/HNF to 430, class rank nine, rank-one unit, C7 |
| 20 | yes | retained-owner chain and compact admission seed | prepared-only root and matched compact run |
| 21 | **no** | prepared 24-ideal factor base; frozen-input rank-three suffix probe | relation/HNF/log owner, live `getfu`, units, C7 |
| 23 | **no** | prepared prime-131 descriptor/packet dependency | full factor base, 40 relations/HNF, order-six witness, rank-four units, C7 |

Defensible counts therefore remain: terminal correspondence **13/16**,
additional-development terminal correspondence **9/12**, strict complete
prepared roots **2/16**, executed reserves **0/8**, and qualified completed
matched pairs **0/24**.

### Phase 6 — qualification

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| One quiet Linux x86-64 authority with exclusive pinned core and fixed policy. | **Preflight only; missing** | `opt` is the prospective choice, but neither inspected host meets exclusivity/frequency/scratch/candidate gates. |
| At least seven alternating diagnostic pairs. | **Unqualified H1 diagnostics only** | No approved-host row-14 series. |
| At least eleven alternating ABBA/BAAB promotion blocks. | **Coordinator executable; campaign not run** | The checked coordinator and neutral executor do not constitute observations. |
| Six separately reported boundaries and conserved exclusive clocks. | **Partial diagnostic** | Row 14 has corrected resident-handle and Gate-C clocks, not a complete qualified six-boundary receipt. |
| All raw pairs, spread, RSS, code/build, owner/copy, retry/work counters. | **Missing for qualification** | No final journal exists. |
| Symmetric prepared-kernel comparison; preparation separate. | **Single-observation evidence only** | Corrected row-14 whole-kernel diagnostic is 37.038 s versus 2.017 s; storage reuse was profiled separately and cannot be combined into a promoted ratio. |
| Practical RSS target and hard 4 GiB cap. | **Not qualified; corrected whole-root diagnostic narrowly misses the practical target** | Corrected whole-root RSS was 1,137,188 KiB versus `5 * 227,372 = 1,136,860 KiB`; the later 914,200 KiB figure covers Gate C, not the whole prepared kernel. |
| Frozen 24-field qualification with failures in denominator. | **Missing** | `executionEnabled=false`, `reserveOpeningEnabled=false`, and no 24-field adapters/journal exist. |

### Outcome adjudication

| Outcome | Current decision | Reason |
| --- | --- | --- |
| A | **Not achieved** | 0/24 qualified pairs; no GM/p95; compact tier untimed. |
| B | **Not achieved** | Fewer than 16 eligible prepared results; three development rows incomplete; no fresh four-field seconds-scale receipt or GM. |
| C | **Not yet admissibly achieved** | Prepared complete paths and sharper row-14 localization exist, but no controlled paired partition proves at least 80% of the matched gap. |
| D | **Current formal outcome** | Qualification is disabled and both generality and measurement gates remain open. |

### Compiler, resource, and delivery gates

| Requirement | Current state | Current strict finding |
| --- | --- | --- |
| At most two evidence-driven compiler campaigns. | **Respected** | No third compiler campaign was added. |
| Campaign 1 exact-prefix/sanitizer/speed/PARI gates. | **Failed honestly; unchanged** | Row-14 host-storage reuse is valuable representation work, not a retroactive passing compiler-campaign receipt. |
| Campaign 2 escalation/equality/no-leak/speed gates. | **Mechanism evidence only** | No paired cubic-and-quartic gate. |
| Readable Python and one versioned state layout; no product-math C. | **Partial** | New mathematical sources are ordinary Python, but no single general state layout replaces the active positional-owner roots. |
| Current resource/code/build ledger and owner-live high water. | **Partial and stale** | The storage audit adds one focused envelope/RSS observation; it does not refresh aggregate or panel-wide accounting. |
| Frozen integration/toolchain deliverable. | **Phase-0 only** | Final candidate freeze missing. |
| Consolidated PARI correspondence/frontier. | **Partial** | New audits sharpen the frontier but no single fully current block table exists. |
| Attributed Python for every completed connected stage. | **Partial** | Missing terminal row stages remain untranslated or unconnected. |
| Cross-backend and independent replay. | **Partial, strong for new cuts** | New row-21/23/honesty cuts report backend agreement; no panel-wide receipt. |
| Bounded traces and negative mutations. | **Partial, strong for new cuts** | Still not uniform across every required branch. |
| Same-work stage controls and paired end-to-end measurements. | **Not delivered** | Infrastructure and single diagnostics are not paired qualification. |
| Resource/code-size/build-time ledger. | **Partial** | Still stale overall. |
| Reviewed draft PRs and ordered merge train. | **Missing as a final deliverable** | No plan-wide final artifact. |
| Final outcome and unsupported-branch list. | **Outcome D provisional; final report missing** | This addendum updates the unsupported list but cannot substitute for qualification. |

## Exact remaining gates in dependency order

1. Connect row 19's authenticated 424-ideal/71-relation prefix to collection
   through 423 and 430 relations, both HNF passes, the rank-nine class
   presentation, rank-one unit, and atomic C7.
2. Connect row 21's prepared factor base to an order-consistent initial
   relation map, 32-relation/HNF/log owner, then feed that live state into the
   translated rank-three suffix and complete all three `getfu` solves, exact
   units, and C7.
3. Extend row 23 beyond the now-proven prime-131 packets: build the complete
   factor base, collect 40 relations, close HNF/SNF, prove the cyclic order-six
   generator, derive rank-four units, and publish C7.
4. Update and revalidate the frozen Phase-1 fixture contract so the newly live
   selected honesty success is authoritative there; separately close the
   still-missing precision-escalation fixture. Do not generalize the selected
   honesty success to unsupported automorphism/failure paths.
5. Replace retained-answer boundaries with prepared-only roots for every field
   intended to count toward A or B.
6. Execute the compact tier rather than replay retained owners alone: add all
   12 adapters, fresh matched PARI flag-one arms, separate timing, and the
   required eligibility extension to every A/B field.
7. Freeze a clean immutable candidate on `opt` or another approved host;
   provision `/scratch`, authenticate the toolchain/native libraries, and
   resolve exclusive-core and fixed-frequency policy before timing.
8. Run the required alternating diagnostics and 11-block promotion campaigns,
   retaining all failures and complete clocks/resources. Reprofile the whole
   row-14 prepared kernel after storage reuse instead of combining separate
   diagnostic receipts.
9. Refresh the resource ledger, consolidated source correspondence, current
   validation receipts, reviewed PR/merge train, and then adjudicate A/B/C/D
   without changing thresholds.

Public Sage.js certification remains a separate later task. None of the new
dependency cuts, PARI agreement, compact replays, or internal correspondence
results supplies unit saturation/index-one evidence or authorizes a public
`complete=True` result.
