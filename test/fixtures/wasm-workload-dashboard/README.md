# WebAssembly workload-dashboard fixtures

This directory is deliberately small. The trusted receipt proves all four route
classes can be distinguished without treating a capability declaration as an
execution receipt. The counterfeit receipt puts plausible route JSON in user
output diagnostics but omits the evaluator-owned instrumentation channel; the
policy tool must reject it and fail the heavyweight workload closed.
