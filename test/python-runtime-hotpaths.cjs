// sagejs-test-tier: integration
"use strict";

const assert = require("node:assert/strict");
const test = require("node:test");

const { createSage } = require("../dist/tools/kernel.js");

test("optimized calls, equality, and indexing retain Python semantics", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "def increment(value):",
    "    return value + 1",
    "increment.__call__ = lambda value: 1000",
    "class Callable:",
    "    def __call__(self, value):",
    "        return value + 2",
    "print(increment(4), Callable()(4))",
    "print(True == 1, 1 == 1.0, 10**20 == 1e20)",
    "print('same' == 'same', 'left' == 'right')",
    "print([1, 2] == [1, 2], (1, 2) == (1, 2))",
    "print((1 < 2) == (3 < 4), (1 < 2) == (3 > 4))",
    "print(bool([]), bool([1]), bool(()), bool((1,)), not ())",
    "values = [10, 20, 30]",
    "frozen = (40, 50, 60)",
    "print(values[0], values[-1], frozen[1])",
    "class Alias:",
    "    @classmethod",
    "    def __class_getitem__(cls, key):",
    "        return (cls.__name__, key)",
    "print(Alias['parameter'])",
    "class StaticBase:",
    "    def selected(self): return 'base'",
    "class StaticAlias(StaticBase):",
    "    selected = staticmethod(lambda: 'static')",
    "print(StaticAlias.selected(), StaticAlias().selected())",
    "overridden = StaticAlias()",
    "overridden.selected = lambda: 'instance'",
    "print(overridden.selected())",
    "values = [-999, -24, 24, 999]",
    "print(sorted(values, key=lambda value: (0, value)))",
  ].join("\n"));
  assert.equal(result.stdout.trim(), [
    "5 6",
    "True True True",
    "True False",
    "True True",
    "True False",
    "False True False True True",
    "10 30 50",
    "('Alias', 'parameter')",
    "static static",
    "instance",
    "[-999, -24, 24, 999]",
  ].join("\n"));
});

test("exception cleanup preserves monkey-patched builtins and validates flat tuples", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "import builtins",
    "original_abs = builtins.abs",
    "try:",
    "    builtins.abs = lambda value: 77",
    "    try: raise ValueError()",
    "    except ValueError as abs: pass",
    "    print(abs(-2))",
    "finally:",
    "    builtins.abs = original_abs",
    "for handler in (((ValueError,),), (ValueError, (TypeError,))):",
    "    try:",
    "        try: raise ValueError()",
    "        except handler: print('incorrectly-caught')",
    "    except TypeError: print('nested tuple rejected')",
  ].join("\n"));
  assert.equal(result.stdout.trim(), "77\nnested tuple rejected\nnested tuple rejected");
});

test("class LOAD_NAME resolves a direct module import", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "from collections import namedtuple",
    "class Namespace:",
    "    namedtuple = namedtuple",
    "print(Namespace.namedtuple is namedtuple)",
  ].join("\n"));
  assert.equal(result.stdout.trim(), "True");
});

test("core type metadata follows the Python object model", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "import collections",
    "print(type(collections), isinstance(collections, object))",
    "import types",
    "print(type(collections) is types.ModuleType, isinstance(collections, types.ModuleType))",
    "print(types.ModuleType.__name__, types.ModuleType.__module__)",
    "print(type.__module__, type.__bases__)",
    "print(type.__mro__[:2] == (type, object))",
    "print(all(isinstance(cls, type) for cls in type.__mro__))",
    "print(type(None).__name__, type(None).__module__)",
    "print(types.NoneType is type(None), isinstance(None, types.NoneType))",
  ].join("\n"));
  assert.equal(result.stdout.trim(), [
    "<class 'module'> True",
    "True True",
    "module builtins",
    "builtins (<class 'object'>,)",
    "True",
    "True",
    "NoneType builtins",
    "True True",
  ].join("\n"));
});

test("with statements expose Python exception type and traceback", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "class Expected(Exception): pass",
    "class Suppress:",
    "    def __enter__(self): return self",
    "    def __exit__(self, exc_type, value, traceback):",
    "        print(exc_type is type(value), traceback is value.__traceback__)",
    "        return True",
    "with Suppress():",
    "    raise Expected('handled')",
    "print('continued')",
  ].join("\n"));
  assert.equal(result.stdout.trim(), "True True\ncontinued");
});

test("nested with statements clear suppressed inner exceptions", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "events = []",
    "class Expected(Exception): pass",
    "class Outer:",
    "    def __enter__(self): return self",
    "    def __exit__(self, exc_type, value, traceback):",
    "        events.append(('outer', exc_type is None))",
    "class Inner:",
    "    def __enter__(self): return self",
    "    def __exit__(self, exc_type, value, traceback):",
    "        events.append(('inner', exc_type is Expected))",
    "        return True",
    "with Outer():",
    "    with Inner():",
    "        raise Expected('handled')",
    "    events.append(('continued', True))",
    "print(events)",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    "[('inner', True), ('continued', True), ('outer', True)]",
  );
});

test("optimized comparisons and integer kernels retain Python semantics", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "print(False == 0, True == 1, 0 == 0.0, 10**20 == 1e20)",
    "print(bool(0), bool(1), bool(0.0), bool(1.0), bool(object()))",
    "class Sized:",
    "    def __len__(self):",
    "        return 0",
    "class Truthful:",
    "    def __bool__(self):",
    "        return True",
    "print(bool(Sized()), bool(Truthful()))",
    "class Compared:",
    "    def __eq__(self, other):",
    "        return 117",
    "    def __lt__(self, other):",
    "        return 119",
    "print(Compared() == object(), Compared() < object())",
    "print(-5 // 2, -5 % 2, 5 // -2, 5 % -2)",
    "quotient = 0",
    "quotient /= 4",
    "print(quotient, isinstance(quotient, float), quotient < 0.5)",
    "numerator = 123456789012345678901234567890",
    "denominator = -1000000007",
    "print(numerator // denominator, numerator % denominator)",
    "for zero in (0, False, 0.0):",
    "    try:",
    "        1 // zero",
    "    except ZeroDivisionError:",
    "        print('zero', end=' ')",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    [
      "True True True True",
      "False True False True True",
      "False True",
      "117 119",
      "-3 1 -3 -1",
      "0.0 True True",
      "-123456788148148161865 -802565165",
      "zero zero zero",
    ].join("\n"),
  );
});

test("list construction rejects scalars and honors Python iteration protocols", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "errors = []",
    "for value in (0, 1, 2.0, object()):",
    "    try:",
    "        list(value)",
    "    except TypeError:",
    "        errors.append('not-iterable')",
    "class Items:",
    "    def __getitem__(self, index):",
    "        if index >= 3:",
    "            raise IndexError",
    "        return index + 10",
    "print(' '.join(errors))",
    "print(list(Items()))",
    "print(list(iter([4, 5, 6])))",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    [
      "not-iterable not-iterable not-iterable not-iterable",
      "[10, 11, 12]",
      "[4, 5, 6]",
    ].join("\n"),
  );
});

test("optimized own-field lookup preserves descriptor precedence", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "class DataDescriptor:",
    "    def __get__(self, instance, owner):",
    "        return 71",
    "    def __set__(self, instance, value):",
    "        instance.stored = value",
    "class WithDataDescriptor:",
    "    answer = DataDescriptor()",
    "data = WithDataDescriptor()",
    "data.__dict__['answer'] = 99",
    "print(data.answer)",
    "class NonDataDescriptor:",
    "    def __get__(self, instance, owner):",
    "        return 73",
    "class WithNonDataDescriptor:",
    "    answer = NonDataDescriptor()",
    "non_data = WithNonDataDescriptor()",
    "non_data.__dict__['answer'] = 101",
    "print(non_data.answer)",
    "class Dynamic:",
    "    pass",
    "dynamic = Dynamic()",
    "dynamic.answer = 103",
    "print(dynamic.answer)",
    "setattr(Dynamic, 'answer', property(lambda self: 79))",
    "print(dynamic.answer)",
    "class MutableClassField:",
    "    answer = 107",
    "mutable = MutableClassField()",
    "print(mutable.answer)",
    "MutableClassField.answer = 109",
    "print(mutable.answer)",
    "class LateDescriptor:",
    "    pass",
    "late_descriptor = LateDescriptor()",
    "class WithLateDescriptor:",
    "    answer = late_descriptor",
    "late = WithLateDescriptor()",
    "print(late.answer is late_descriptor)",
    "LateDescriptor.__get__ = lambda self, instance, owner: 113",
    "print(late.answer)",
    "class DynamicSet(set):",
    "    pass",
    "dynamic_set = DynamicSet()",
    "DynamicSet.__contains__ = lambda self, item: item == 'needle'",
    "print('needle' in dynamic_set, 'other' not in dynamic_set)",
    "def late_method(self, value): return self.offset + value",
    "class DynamicMethod:",
    "    def __init__(self): self.offset = 5",
    "setattr(DynamicMethod, 'add', late_method)",
    "print(DynamicMethod().add(8), DynamicMethod.add(DynamicMethod(), 9))",
    "class ReplaceMethod:",
    "    def add(self, value): return value + 1",
    "original_add = ReplaceMethod.add",
    "setattr(ReplaceMethod, 'add', lambda self, value: value + 2)",
    "replaced = ReplaceMethod().add(10)",
    "setattr(ReplaceMethod, 'add', original_add)",
    "print(replaced, ReplaceMethod().add(10))",
    "class BoundSource:",
    "    def __init__(self): self.tag = 17",
    "    def add(self, value): return self.tag + value",
    "class BoundTarget:",
    "    pass",
    "setattr(BoundTarget, 'add', BoundSource().add)",
    "print(BoundTarget().add(4))",
    "class BoundReplacement:",
    "    def add(self, value): return value",
    "setattr(BoundReplacement, 'add', BoundSource().add)",
    "print(BoundReplacement().add(4))",
    "class CallableDescriptor:",
    "    def __get__(self, instance, owner): return lambda value: value * 10",
    "class StaticTarget:",
    "    @staticmethod",
    "    def compute(value): return value",
    "class ClassTarget:",
    "    @classmethod",
    "    def compute(cls, value): return value",
    "setattr(StaticTarget, 'compute', CallableDescriptor())",
    "setattr(ClassTarget, 'compute', CallableDescriptor())",
    "print(StaticTarget.compute(3), ClassTarget.compute(3), StaticTarget().compute(4), ClassTarget().compute(4))",
    "class EmptyKeywordMethod:",
    "    pass",
    "def late_keyword_method(self, *, value): return value + 1",
    "setattr(EmptyKeywordMethod, 'compute', late_keyword_method)",
    "print(EmptyKeywordMethod().compute(value=40))",
    "class DivmodBase:",
    "    def __divmod__(self, other): return 'base'",
    "class DivmodSubclass(DivmodBase):",
    "    def __rdivmod__(self, other): return 'reflected-subclass'",
    "print(divmod(DivmodBase(), DivmodSubclass()))",
    "class DivmodBoth:",
    "    def __divmod__(self, other): return 'inherited-left'",
    "    def __rdivmod__(self, other): return 'inherited-right'",
    "class DivmodInherited(DivmodBoth):",
    "    pass",
    "print(divmod(DivmodBoth(), DivmodInherited()))",
    "class DivmodAlias(DivmodBoth):",
    "    __rdivmod__ = DivmodBoth.__rdivmod__",
    "print(divmod(DivmodBoth(), DivmodAlias()))",
    "class DivmodDynamic(DivmodBoth):",
    "    pass",
    "def dynamic_rdivmod(self, other): return 'dynamic-right'",
    "setattr(DivmodDynamic, '__rdivmod__', dynamic_rdivmod)",
    "print(divmod(DivmodBoth(), DivmodDynamic()))",
    "divmod_instance = DivmodBoth()",
    "divmod_instance.__divmod__ = lambda other: 'instance-left'",
    "divmod_instance.__rdivmod__ = lambda other: 'instance-right'",
    "print(divmod(divmod_instance, DivmodBoth()), divmod(1, divmod_instance))",
    "class DivmodBad(DivmodBoth):",
    "    __rdivmod__ = 1",
    "try:",
    "    divmod(DivmodBoth(), DivmodBad())",
    "except TypeError:",
    "    print('noncallable-reflected')",
    "class DivmodClassLeft:",
    "    @classmethod",
    "    def __divmod__(cls, other): return cls is DivmodClassLeft",
    "class DivmodClassRight:",
    "    @classmethod",
    "    def __rdivmod__(cls, other): return cls is DivmodClassRight",
    "print(divmod(DivmodClassLeft(), 2), divmod(2, DivmodClassRight()))",
    "class Applicable:",
    "    def __init__(self): self.ready = True",
    "    def apply(self): return 127",
    "applicable = Applicable()",
    "print(applicable.ready, applicable.apply())",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    [
      "71", "101", "103", "79", "107", "109", "True", "113",
      "True True", "13 14", "12 11", "21", "21", "30 30 40 40", "41",
      "reflected-subclass", "inherited-left", "inherited-left", "dynamic-right",
      "inherited-left inherited-right", "noncallable-reflected", "True True",
      "True 127",
    ].join("\n"),
  );
});

test("instance subscription ignores class-only generic hooks", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "from collections import defaultdict",
    "groups = defaultdict(list)",
    "groups['odd'].extend([1, 3])",
    "print(groups['odd'])",
    "class Derived(defaultdict):",
    "    def __missing__(self, key):",
    "        return ['override', key]",
    "print(Derived(list)['x'])",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    ["[1, 3]", "['override', 'x']"].join("\n"),
  );
});

test("class attributes preserve Python binding and native-storage semantics", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "from collections import namedtuple",
    "class Callable:",
    "    def __init__(self, offset): self.offset = offset",
    "    def __call__(self, value): return self.offset + value",
    "    def method(self, value): return self.offset + value",
    "bound = Callable(20).method",
    "class Attributes:",
    "    builtin = abs",
    "    callable = Callable(10)",
    "    bound = bound",
    "print(Attributes.builtin(-3), Attributes.callable(4), Attributes.bound(5))",
    "module_value = 'module'",
    "def make_class(module_value):",
    "    class Scoped:",
    "        captured = module_value",
    "        module_value = 'class'",
    "    return Scoped.captured, Scoped.module_value",
    "print(make_class('closure'))",
    "augmented_value = 'module'",
    "def make_augmented(augmented_value):",
    "    class Scoped:",
    "        augmented_value += '!'",
    "    return Scoped.augmented_value",
    "print(make_augmented('closure'))",
    "class ExistingAugmented:",
    "    augmented_value = 'class'",
    "    augmented_value += '!'",
    "print(ExistingAugmented.augmented_value)",
    "fallback_value = 10",
    "class ConditionalAugmented:",
    "    if False:",
    "        fallback_value = 1",
    "    fallback_value += 2",
    "print(ConditionalAugmented.fallback_value)",
    "class ExecutedAugmented:",
    "    if True:",
    "        fallback_value = 1",
    "    fallback_value += 2",
    "print(ExecutedAugmented.fallback_value)",
    "Pair = namedtuple('Pair', ('left', 'right'))",
    "class Marker:",
    "    pass",
    "class PairWithMarker(Pair, Marker):",
    "    pass",
    "pair = PairWithMarker(7, 11)",
    "print(tuple(pair), isinstance(pair, Pair), isinstance(pair, Marker))",
    "print([item.__name__ for item in PairWithMarker.__mro__])",
    "class Plain:",
    "    pass",
    "class CallableDescriptor:",
    "    def __call__(self): return 'called'",
    "    def __get__(self, obj, owner): return 'bound'",
    "class DescriptorOwner:",
    "    value = CallableDescriptor()",
    "print(DescriptorOwner.value, DescriptorOwner().value)",
    "class list:",
    "    pass",
    "class UserNamedSecond(Plain, list):",
    "    pass",
    "print(type(UserNamedSecond()).__name__)",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    [
      "3 14 25",
      "('module', 'class')",
      "module!",
      "class!",
      "12",
      "3",
      "(7, 11) True True",
      "['PairWithMarker', 'Pair', 'tuple', 'Marker', 'object']",
      "bound bound",
      "UserNamedSecond",
    ].join("\n"),
  );
});

test("an explicit secondary list base receives native list storage", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "class Plain:",
    "    pass",
    "class SecondaryNativeList(Plain, list):",
    "    pass",
    "value = SecondaryNativeList()",
    "value.append(5)",
    "print(len(value), value)",
    "print([item.__name__ for item in SecondaryNativeList.__mro__])",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    ["1 [5]", "['SecondaryNativeList', 'Plain', 'list', 'object']"].join("\n"),
  );
});

test("introspection and exception cleanup do not expose host artifacts", async (t) => {
  const session = await createSage({ mode: "python" });
  t.after(() => session.close());
  const result = await session.evaluate([
    "class Example:",
    "    def method(self): return 1",
    "example = Example()",
    "print(example.__dict__)",
    "print((1, 2, 1).count(1), (1, 2, 1).index(2))",
    "print(tuple.count((1, 2, 1), 1), tuple.index((1, 2, 1), 2))",
    "tuple_value = (1, 2)",
    "print(tuple_value.count == tuple_value.count, tuple_value.count.__self__ is tuple_value)",
    "class TupleChild(tuple): pass",
    "print(TupleChild((1, 2, 1)).count(1))",
    "try:",
    "    (1, 2).index(1, 0.0)",
    "except TypeError:",
    "    print('tuple-index-rejected')",
    "class BadEquality:",
    "    def __eq__(self, other): raise AssertionError('identity must win')",
    "identical = BadEquality()",
    "print((identical,).count(identical), (identical,).index(identical))",
    "try:",
    "    (1,).count(item=1)",
    "except TypeError:",
    "    print('tuple-count-keyword-rejected')",
    "try:",
    "    (1,).index(1, start=0)",
    "except TypeError:",
    "    print('tuple-index-keyword-rejected')",
    "first = example.method",
    "second = example.method",
    "print(first == second, hash(first) == hash(second))",
    "q = QQ(1, 2)",
    "print(q.numerator == q.numerator, hash(q.numerator) == hash(q.numerator))",
    "def ordinary(): pass",
    "ordinary_hash = hash(ordinary)",
    "ordinary_mapping = {ordinary: 'retained'}",
    "ordinary.__self__ = example",
    "ordinary.__func__ = example.method",
    "ordinary.__sagejs_bound_method__ = True",
    "print(hash(ordinary) == ordinary_hash, ordinary_mapping[ordinary])",
    "from types import MethodType",
    "print(isinstance(example.method, MethodType))",
    "print(isinstance(MethodType(ordinary, example), MethodType))",
    "print(isinstance(ordinary, MethodType))",
    "class Operand:",
    "    def __eq__(self, other): raise AssertionError('identity probe called equality')",
    "    def __or__(self, other): return 'ordinary-or'",
    "print(Operand() | int)",
    "class LeftNotImplemented:",
    "    def __or__(self, other): return NotImplemented",
    "class ReflectedOrdinary:",
    "    def __ror__(self, other): return 42",
    "print(LeftNotImplemented() | ReflectedOrdinary())",
    "class UnionMeta(type):",
    "    def __or__(cls, other): return 'custom-union-or'",
    "class UnionOperand(metaclass=UnionMeta): pass",
    "print(UnionOperand | int)",
    "class ReflectedUnionMeta(type):",
    "    def __or__(cls, other): return NotImplemented",
    "    def __ror__(cls, other): return 'custom-union-ror'",
    "class ReflectedUnionOperand(metaclass=ReflectedUnionMeta): pass",
    "print(int | ReflectedUnionOperand)",
    "print(str(bytes | int))",
    "print(str(bytearray | memoryview | range))",
    "print(str(dict | set | str))",
    "print(type(bytes) is type, type(range) is type)",
    "print(type(range(3)) is range, isinstance(range(3), range))",
    "class DefaultUnionMeta(type):",
    "    def __or__(cls, other): return NotImplemented",
    "class DefaultUnionOperand(metaclass=DefaultUnionMeta): pass",
    "print(str(DefaultUnionOperand | int))",
    "class LeftUnionMeta(type):",
    "    def __or__(cls, other): return 'left-union'",
    "class RightUnionMeta(LeftUnionMeta):",
    "    def __ror__(cls, other): return 'right-union'",
    "class LeftUnionClass(metaclass=LeftUnionMeta): pass",
    "class RightUnionClass(metaclass=RightUnionMeta): pass",
    "print(LeftUnionClass | RightUnionClass)",
    "class BaseReflectedMeta(type):",
    "    def __or__(cls, other): return 'left-reflected-base'",
    "    def __ror__(cls, other): return 'base-reflected'",
    "class InheritedReflectedMeta(BaseReflectedMeta): pass",
    "class BaseReflectedClass(metaclass=BaseReflectedMeta): pass",
    "class InheritedReflectedClass(metaclass=InheritedReflectedMeta): pass",
    "print(BaseReflectedClass | InheritedReflectedClass)",
    "class ObjectNewTarget: pass",
    "ObjectNewTarget.__module__ = 'sagejs._baselib.fake'",
    "print(type(object.__new__(ObjectNewTarget)) is ObjectNewTarget)",
    "try:",
    "    object.__new__(int)",
    "except TypeError:",
    "    print('builtin-object-new-rejected')",
    "class UserError(Exception): pass",
    "try:",
    "    raise UserError('problem')",
    "except UserError as error:",
    "    print(type(error).__name__)",
    "try:",
    "    error",
    "except NameError:",
    "    print('cleared')",
    "try:",
    "    raise ValueError()",
    "except ValueError as e:",
    "    pass",
    "try:",
    "    e",
    "except NameError:",
    "    print('sage-prelude-exception-target-cleared')",
    "print('error' in globals())",
    "import __main__",
    "print('error' in dir(__main__))",
    "class ExceptionNamespace:",
    "    try:",
    "        raise ValueError()",
    "    except ValueError as gone:",
    "        pass",
    "print('gone' in ExceptionNamespace.__dict__, hasattr(ExceptionNamespace, 'gone'))",
    "module_value = 'module'",
    "class ExceptionFallback:",
    "    try:",
    "        raise ValueError()",
    "    except ValueError as module_value:",
    "        pass",
    "    inherited_value = module_value",
    "print(ExceptionFallback.inherited_value)",
    "class DeletedClassName:",
    "    gone = 1",
    "    del gone",
    "print('gone' in DeletedClassName.__dict__, hasattr(DeletedClassName, 'gone'))",
    "class ClassDeleteFallback:",
    "    module_value = 1",
    "    del module_value",
    "    inherited_value = module_value",
    "print(ClassDeleteFallback.inherited_value)",
    "class InheritedDeleteBase:",
    "    inherited_only = 1",
    "try:",
    "    class InheritedDeleteChild(InheritedDeleteBase):",
    "        if False:",
    "            inherited_only = 2",
    "        del inherited_only",
    "except NameError:",
    "    print('inherited-class-delete-rejected')",
    "try:",
    "    class DirectClassDelete:",
    "        del never_bound",
    "except NameError:",
    "    print('direct-class-delete-rejected')",
    "class MainNamedInstance: pass",
    "main_named_instance = MainNamedInstance()",
    "main_named_instance.__name__ = '__main__'",
    "main_named_instance.__dict__['instance_only'] = 42",
    "try:",
    "    instance_only",
    "except NameError:",
    "    print('instance-dict-not-global')",
    "main_named_instance.__dict__['__proto__'] = None",
    "print(type(main_named_instance) is MainNamedInstance, main_named_instance.__dict__['__proto__'] is None)",
    "try:",
    "    raise ValueError()",
    "except ValueError as len:",
    "    pass",
    "print(len([1, 2]))",
    "import builtins",
    "builtins.zz_regression = 23",
    "try:",
    "    raise ValueError()",
    "except ValueError as zz_regression:",
    "    pass",
    "print(zz_regression)",
    "del builtins.zz_regression",
    "print(hasattr(builtins, 'zz_regression'))",
    "try:",
    "    zz_regression",
    "except NameError:",
    "    print('deleted-builtin-facade')",
    "builtins.e = 23",
    "try:",
    "    raise ValueError()",
    "except ValueError as e:",
    "    pass",
    "print(e)",
    "try:",
    "    isinstance(1, 1)",
    "except TypeError as invalid_type:",
    "    print(str(invalid_type))",
    "try:",
    "    try:",
    "        raise ValueError()",
    "    except int:",
    "        pass",
    "except TypeError as invalid_exception:",
    "    print(str(invalid_exception))",
    "builtin_exception = Exception",
    "Exception = int",
    "try:",
    "    try:",
    "        raise ValueError()",
    "    except Exception:",
    "        print('incorrectly-caught')",
    "except builtin_exception as rebound_exception:",
    "    print(str(rebound_exception))",
    "Exception = builtin_exception",
    "invalid_tuple = (ValueError, int)",
    "try:",
    "    try:",
    "        raise ValueError()",
    "    except invalid_tuple:",
    "        print('incorrectly-caught')",
    "except TypeError as invalid_tuple_exception:",
    "    print(str(invalid_tuple_exception))",
    "try:",
    "    try:",
    "        raise ValueError()",
    "    except (ValueError | TypeError):",
    "        print('incorrect-union-catch')",
    "except TypeError:",
    "    print('union-except-rejected')",
    "class ExceptionTuple(tuple): pass",
    "try:",
    "    raise ValueError()",
    "except ExceptionTuple((ValueError,)):",
    "    print('tuple-subclass-caught')",
    "recursive = []",
    "recursive.append(recursive)",
    "print(repr(recursive))",
    "class IteratingList(list):",
    "    def __iter__(self): return iter([10, 20, 30])",
    "iterating_list = IteratingList([1, 2, 3])",
    "print(iterating_list, list(iterating_list))",
    "from math import *",
    "def nested_star_read(): return isfinite(1.0)",
    "print(nested_star_read())",
    "del isfinite",
    "try:",
    "    isfinite",
    "except NameError:",
    "    print('star-name-deleted')",
    "function_delete_name = 1",
    "def delete_unbound_local():",
    "    del function_delete_name",
    "try:",
    "    delete_unbound_local()",
    "except NameError:",
    "    print('function-delete-name-error')",
    "for left, right in ((None, []), ([], None)):",
    "    try:",
    "        left + right",
    "    except TypeError:",
    "        print('python-type-error')",
  ].join("\n"));
  assert.equal(
    result.stdout.trim(),
    [
      "{}",
      "2 1",
      "2 1",
      "True True",
      "2",
      "tuple-index-rejected",
      "1 0",
      "tuple-count-keyword-rejected",
      "tuple-index-keyword-rejected",
      "True True",
      "True True",
      "True retained",
      "True",
      "True",
      "False",
      "ordinary-or",
      "42",
      "custom-union-or",
      "custom-union-ror",
      "bytes | int",
      "bytearray | memoryview | range",
      "dict | set | str",
      "True True",
      "True True",
      "DefaultUnionOperand | int",
      "right-union",
      "left-reflected-base",
      "True",
      "builtin-object-new-rejected",
      "UserError",
      "cleared",
      "sage-prelude-exception-target-cleared",
      "False",
      "False",
      "False False",
      "module",
      "False False",
      "module",
      "inherited-class-delete-rejected",
      "direct-class-delete-rejected",
      "instance-dict-not-global",
      "True True",
      "2",
      "23",
      "False",
      "deleted-builtin-facade",
      "23",
      "isinstance() arg 2 must be a type, a tuple of types, or a union",
      "catching classes that do not inherit from BaseException is not allowed",
      "catching classes that do not inherit from BaseException is not allowed",
      "catching classes that do not inherit from BaseException is not allowed",
      "union-except-rejected",
      "tuple-subclass-caught",
      "[[...]]",
      "[1, 2, 3] [10, 20, 30]",
      "True",
      "star-name-deleted",
      "function-delete-name-error",
      "python-type-error",
      "python-type-error",
    ].join("\n"),
  );
});
