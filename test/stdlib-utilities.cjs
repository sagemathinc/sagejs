"use strict";

// Focused compatibility vectors adapted from CPython's test_collections,
// test_functools, test_statistics, test_bisect, test_heapq, and
// test_contextlib suites.
const assert = require("node:assert/strict");

const { createSage } = require("../dist/tools/kernel.js");

async function testUtilityModules() {
  const session = await createSage({ mode: "python" });
  try {
    const result = await session.evaluate(
      [
        "from collections import Counter, defaultdict, ChainMap, OrderedDict, deque, namedtuple",
        "counter = Counter('abracadabra')",
        "print(counter.most_common(3), counter.total(), sorted(counter.elements()))",
        "counter.subtract('aaaaz'); print(counter['a'], counter['z'], counter['missing'])",
        "print(Counter('ab') + Counter('bcc'))",
        "print(Counter('abbccc').most_common(True), Counter('abbccc').most_common(-1))",
        "try: Counter('abbccc').most_common(2.5)",
        "except Exception as error: print('most-common-float', isinstance(error, TypeError), str(error))",
        "try: Counter.fromkeys('abc', 2)",
        "except Exception as error: print(type(error).__name__, str(error))",
        "class CounterIndex:",
        "    def __index__(self): return 2",
        "indexed_counts = Counter(b=0, c=-1); indexed_counts['a'] = CounterIndex()",
        "print(list(indexed_counts.elements()))",
        "try: list(Counter(a=2.5).elements())",
        "except Exception as error: print('elements-float', isinstance(error, TypeError), str(error))",
        "equal_counts = Counter(a=1)",
        "zero_count = Counter(a=1, b=0)",
        "larger_counts = Counter(a=2, b=1)",
        "negative_count = Counter(a=1, b=-1)",
        "for left, right in [(equal_counts, zero_count), (equal_counts, larger_counts), (negative_count, equal_counts)]:",
        "    print(left == right, left != right, left < right, left <= right, left > right, left >= right)",
        "print(Counter.__eq__(equal_counts, {'a': 1}) is NotImplemented, Counter.__le__(equal_counts, {'a': 1}) is NotImplemented)",
        "copied_counts = Counter(a=2, b=-1).copy(); copied_counts['a'] = 9",
        "print(Counter(a=2, b=-1), copied_counts)",
        "print([getattr(Counter(a=1), method)({'a': 1}) is NotImplemented for method in ('__add__', '__sub__', '__and__', '__or__')])",
        "inplace_add = Counter(a=2, b=-1); add_identity = inplace_add; inplace_add += Counter(a=-2, c=3, d=0)",
        "print(inplace_add is add_identity, list(inplace_add.items()))",
        "inplace_subtract = Counter(a=2, b=-1); subtract_identity = inplace_subtract; inplace_subtract -= Counter(a=3, c=-2, d=0)",
        "print(inplace_subtract is subtract_identity, list(inplace_subtract.items()))",
        "inplace_intersection = Counter(a=2, b=-1); intersection_identity = inplace_intersection; inplace_intersection &= Counter(a=1, c=3, d=0)",
        "print(inplace_intersection is intersection_identity, list(inplace_intersection.items()))",
        "inplace_union = Counter(a=2, b=-1); union_identity = inplace_union; inplace_union |= Counter(a=1, c=3, d=0)",
        "print(inplace_union is union_identity, list(inplace_union.items()))",
        "mapping_update = Counter(a=2); mapping_identity = mapping_update; mapping_update += {'a': 1, 'b': 3}",
        "print(mapping_update is mapping_identity, list(mapping_update.items()))",
        "groups = defaultdict(list); groups['odd'].extend([1, 3]); print(groups['odd'], groups['even'])",
        "copied_groups = groups.copy(); copied_groups['odd'].append(5); copied_groups['new'].append(7)",
        "class FancyDefaultDict(defaultdict): pass",
        "fancy_groups = FancyDefaultDict(int, [('a', 1)]).copy()",
        "print(copied_groups is not groups, copied_groups.default_factory is list, list(groups.items()), list(copied_groups.items()), isinstance(fancy_groups, FancyDefaultDict), fancy_groups.default_factory is int)",
        "mapping = ChainMap({'x': 1}, {'x': 2, 'y': 3})",
        "mapping['z'] = 4; print(mapping['x'], mapping['y'], mapping['z'], sorted(mapping))",
        "chain_calls = []",
        "chain_fallback = defaultdict(lambda: chain_calls.append('factory') or 42)",
        "print(ChainMap({}, chain_fallback)['missing'], chain_calls, list(chain_fallback.items()))",
        "class MissingChainMap(ChainMap):",
        "    def __missing__(self, key): return ('missing', key)",
        "class RaisingMembership(dict):",
        "    def __contains__(self, key): raise AssertionError('membership should not be tested')",
        "print(MissingChainMap({})['absent'], ChainMap(RaisingMembership(answer=17))['answer'])",
        "get_calls = []",
        "get_fallback = defaultdict(lambda: get_calls.append('factory') or 42)",
        "print(ChainMap({}, get_fallback).get('missing', 'default'), get_calls, list(get_fallback.items()))",
        "first_map = {'first': 1}; lower_map = {'lower': 2}",
        "mutable_chain = ChainMap(first_map, lower_map)",
        "print(mutable_chain.setdefault('lower', 9), mutable_chain.setdefault('missing', 9), first_map)",
        "try: del mutable_chain['lower']",
        "except Exception as error: print('chain-del-lower', isinstance(error, KeyError), error.args, lower_map)",
        "mutable_chain.update({'mapped': 3}); mutable_chain.update([('pair', 4)], keyword=5); print(first_map, lower_map)",
        "print(mutable_chain.pop('first'), mutable_chain.pop('lower', 'fallback'), first_map, lower_map)",
        "try: mutable_chain.pop('lower')",
        "except Exception as error: print('chain-pop-lower', isinstance(error, KeyError), error.args)",
        "first_map['last'] = 5; print(mutable_chain.popitem(), first_map, lower_map)",
        "first_map.clear()",
        "try: mutable_chain.popitem()",
        "except Exception as error: print('chain-popitem-empty', isinstance(error, KeyError), error.args)",
        "first_map.update(a=1, b=2); mutable_chain.clear(); print(first_map, lower_map, mutable_chain['lower'])",
        "class FancyChainMap(ChainMap): pass",
        "fancy_chain = FancyChainMap({'top': 1}, lower_map); copied_chain = fancy_chain.copy(); copied_chain['new'] = 6; lower_map['late'] = 7",
        "print(bool(ChainMap()), bool(ChainMap({}, lower_map)), repr(fancy_chain), isinstance(copied_chain, FancyChainMap), copied_chain.maps[0] is fancy_chain.maps[0], copied_chain.maps[1] is lower_map)",
        "ordered = OrderedDict([('a', 1), ('b', 2)])",
        "reordered = OrderedDict([('b', 2), ('a', 1)])",
        "print(ordered == ordered.copy(), ordered == reordered, ordered == {'b': 2, 'a': 1}, OrderedDict.__eq__(ordered, [('a', 1), ('b', 2)]) is NotImplemented)",
        "default_reordered = defaultdict(int, [('b', 2), ('a', 1)])",
        "print(ordered == default_reordered, default_reordered == reordered)",
        "ordered.move_to_end('a'); print(list(ordered.items()))",
        "ordered.move_to_end('a', last=False); print(list(ordered.items()), list(reversed(ordered)))",
        "try: ordered.move_to_end('missing')",
        "except Exception as error: print('move-missing', isinstance(error, KeyError), str(error))",
        "for candidate in (OrderedDict([('a', 1)]), OrderedDict()):",
        "    try: candidate.pop('a', 2, 3)",
        "    except Exception as error: print('pop-defaults', isinstance(error, TypeError), str(error), list(candidate.items()))",
        "queue = deque([1, 2, 3], maxlen=4); queue.rotate(); queue.appendleft(0); print(queue, queue.count(3), queue.index(1))",
        "class DequeIndex:",
        "    def __init__(self, value): self.value = value",
        "    def __index__(self): return self.value",
        "queue = deque([0, 1, 2]); queue.rotate(DequeIndex(-1)); queue.insert(DequeIndex(-1), 9); print(queue)",
        "print(deque([0, 1, 2, 3]).index(2, -20, 20), deque([0, 1, 2, 3]).index(2, 0, -1), deque([0, 1, 2, 3]).index(2, DequeIndex(1), DequeIndex(3)))",
        "print(deque([0, 1, 2])[DequeIndex(-1)])",
        "deletions = deque('abcde')",
        "del deletions[1]; del deletions[-1]; del deletions[DequeIndex(0)]; print(deletions)",
        "try: del deletions[9]",
        "except Exception as error: print('delete-range', isinstance(error, IndexError), str(error))",
        "lower_deque = deque([1, 2])",
        "equal_deque = deque([1, 2], maxlen=5)",
        "higher_deque = deque([1, 3])",
        "print(lower_deque == equal_deque, lower_deque != equal_deque, lower_deque < higher_deque, lower_deque <= equal_deque, higher_deque > lower_deque, equal_deque >= lower_deque)",
        "print([getattr(lower_deque, method)([1, 2]) is NotImplemented for method in ('__eq__', '__ne__', '__lt__', '__le__', '__gt__', '__ge__')])",
        "print(deque([1, 2]) + deque([3, 4]), deque([1, 2], maxlen=3) + deque([3, 4]))",
        "print(deque([1, 2]) * 2, 2 * deque([1, 2]), deque([1, 2], maxlen=3) * DequeIndex(3))",
        "class FancyDeque(deque): pass",
        "print(isinstance(FancyDeque([1]) + deque([2]), FancyDeque), isinstance(FancyDeque([1]) * 2, FancyDeque))",
        "repeated = deque([1, 2], maxlen=3); repeated_identity = repeated; repeated *= 3",
        "print(repeated is repeated_identity, repeated)",
        "emptied = deque([1, 2]); emptied_identity = emptied; emptied *= -1",
        "print(emptied is emptied_identity, emptied)",
        "try: deque([1]) + [2]",
        "except Exception as error: print('add-list', isinstance(error, TypeError), str(error))",
        "try: deque([1]) * 1.5",
        "except Exception as error: print('repeat-float', isinstance(error, TypeError), str(error))",
        "try: deque([1], 1.5)",
        "except Exception as error: print('maxlen-float', isinstance(error, TypeError), str(error))",
        "try: deque([0, 1, 2]).rotate(1.5)",
        "except Exception as error: print('rotate-float', isinstance(error, TypeError), str(error))",
        "try: deque([0, 1, 2]).index(1, 0.5)",
        "except Exception as error: print('index-float', isinstance(error, TypeError), str(error))",
        "bounded = deque([1, 2], maxlen=3)",
        "try: bounded.maxlen = 5",
        "except Exception as error: print('maxlen-set', isinstance(error, AttributeError), bounded.maxlen, list(bounded))",
        "try: del bounded.maxlen",
        "except Exception as error: print('maxlen-delete', isinstance(error, AttributeError), bounded.maxlen, list(bounded))",
        "Point = namedtuple('Point', 'x y'); point = Point(2, y=5); print(point, point.x, point[1])",
        "from functools import reduce, partial, lru_cache, cached_property, cmp_to_key, update_wrapper, total_ordering",
        "print(reduce(lambda left, right: left + right, [1, 2, 3]), reduce(lambda left, right: left + right, [], None))",
        "power = partial(pow, 2); print(power(10), power.func is pow, power.args)",
        "@lru_cache(maxsize=2)",
        "def square(value): return value * value",
        "print(square(3), square(3), square(4), square.cache_info())",
        "typed_calls = []",
        "@lru_cache(maxsize=4, typed=True)",
        "def typed_keyword(value):",
        "    typed_calls.append(value)",
        "    return len(typed_calls)",
        "print(typed_keyword(value=1), typed_keyword(value=True), typed_keyword(value=1), typed_keyword.cache_info())",
        "uncached_calls = []",
        "@lru_cache(maxsize=-2)",
        "def uncached(value):",
        "    uncached_calls.append(value)",
        "    return value",
        "print(uncached(5), uncached(5), uncached_calls, uncached.cache_info(), uncached.cache_parameters())",
        "try: lru_cache(maxsize=1.5)",
        "except Exception as error: print('maxsize-float', isinstance(error, TypeError), str(error))",
        "class Sample:",
        "    def __init__(self):",
        "        self.calls = 0",
        "    @cached_property",
        "    def value(self):",
        "        self.calls += 1",
        "        return 17",
        "sample = Sample(); print(sample.value, sample.value, sample.calls)",
        "print(sorted(['bbb', 'a', 'cc'], key=cmp_to_key(lambda left, right: len(left) - len(right))))",
        "def wrapped(): pass",
        "wrapped.source = 17; wrapped.shared = 'wrapped'",
        "def wrapper(): pass",
        "wrapper.local = 23; wrapper.shared = 'wrapper'",
        "result = update_wrapper(wrapper, wrapped)",
        "print(result is wrapper, wrapper.source, wrapper.local, wrapper.shared, wrapper.__wrapped__ is wrapped)",
        "class AttributeBag:",
        "    def __init__(self, values): self.values = dict(values)",
        "    def update(self, values): self.values.update(values)",
        "class WrappedAttributes:",
        "    bag = {'source': 17, 'shared': 'wrapped'}",
        "class WrapperAttributes:",
        "    bag = AttributeBag({'local': 23, 'shared': 'wrapper'})",
        "custom_wrapper = WrapperAttributes()",
        "custom_result = update_wrapper(custom_wrapper, WrappedAttributes(), assigned=(), updated=('bag',))",
        "print(custom_result is custom_wrapper, custom_wrapper.bag.values)",
        "class OrderedBase:",
        "    def __init__(self, value): self.value = value",
        "    def __eq__(self, other): return self.value == other.value",
        "@total_ordering",
        "class OrderedLT(OrderedBase):",
        "    def __lt__(self, other): return self.value < other.value",
        "@total_ordering",
        "class OrderedLE(OrderedBase):",
        "    def __le__(self, other): return self.value <= other.value",
        "@total_ordering",
        "class OrderedGT(OrderedBase):",
        "    def __gt__(self, other): return self.value > other.value",
        "@total_ordering",
        "class OrderedGE(OrderedBase):",
        "    def __ge__(self, other): return self.value >= other.value",
        "for Ordered in (OrderedLT, OrderedLE, OrderedGT, OrderedGE):",
        "    low = Ordered(1); same = Ordered(1); high = Ordered(2)",
        "    print(low < high, low <= same, high > low, high >= same)",
        "class Unordered: pass",
        "try: total_ordering(Unordered)",
        "except Exception as error: print(type(error).__name__, str(error))",
        "import statistics",
        "print(statistics.mean([1, 2, 3, 4]), statistics.median([1, 9, 3, 7]))",
        "print(statistics.pvariance([1, 2, 3]), statistics.variance([1, 2, 3]))",
        "print(round(statistics.geometric_mean([1, 4, 16]), 6), statistics.harmonic_mean([1, 2, 4]))",
        "print(statistics.harmonic_mean([0, 2], weights=[0, 1]), statistics.harmonic_mean([0, 2], weights=[1, 0]))",
        "for data, weights in [([1, 2], [0, 0]), ([1, 2], [1, -1]), ([1], []), ([1e309], [1])]:",
        "    try: statistics.harmonic_mean(data, weights=weights)",
        "    except Exception as error: print(type(error).__name__, str(error))",
        "print(statistics.mode([1, 2, 2, 3]), statistics.multimode('aabbbbccddd'))",
        "print(statistics.quantiles([1, 2, 3, 4, 5], method='inclusive'))",
        "print(statistics.quantiles([1, 2], method='exclusive'))",
        "print(statistics.quantiles([1, 2, 3], n=10, method='exclusive'))",
        "print(statistics.quantiles([10]))",
        "try: statistics.quantiles([])",
        "except Exception as error: print(type(error).__name__, str(error))",
        "print(statistics.covariance([1, 2, 3], [2, 4, 6]), statistics.correlation([1, 2, 3], [2, 4, 6]))",
        "print(statistics.linear_regression([1, 2, 3], [2, 4, 6]))",
        "import bisect, heapq",
        "values = [1, 3, 5]; bisect.insort(values, 4); print(values, bisect.bisect_left(values, 3), bisect.bisect(values, 3))",
        "class BisectIndex:",
        "    def __init__(self, value): self.value = value",
        "    def __index__(self): return self.value",
        "print(bisect.bisect_left([1, 3, 5], 3, BisectIndex(1)), bisect.bisect_right([1, 3, 5], 3, hi=BisectIndex(2)))",
        "print(bisect.bisect_left([1, 3, 5], 3, True), bisect.bisect_left([1, 3, 5], 3, False))",
        "try: bisect.bisect_left([1, 3, 5], 3, 1.5)",
        "except Exception as error: print('lo-float', isinstance(error, TypeError), str(error))",
        "try: bisect.bisect_left([1, 3, 5], 3, 0, 2.5)",
        "except Exception as error: print('hi-float', isinstance(error, TypeError), str(error))",
        "try: bisect.bisect_left([1, 3, 5], 3, -1)",
        "except Exception as error: print('lo-negative', isinstance(error, ValueError), str(error))",
        "key_calls = []",
        "try: bisect.insort_left([1, 3], 2, lo=1.5, key=lambda value: key_calls.append(value) or value)",
        "except TypeError: pass",
        "print(key_calls)",
        "try: bisect.insort_left([1, 3], 2, lo=-1, key=lambda value: key_calls.append(value) or value)",
        "except ValueError: pass",
        "print(key_calls)",
        "heapq.heapify(values); print([heapq.heappop(values) for _ in range(len(values))])",
        "heap = [3, 5]; heapq.heapify(heap); print(heapq.heappushpop(heap, 4), heapq.nsmallest(2, [9, 1, 7, 2]))",
        "print(list(heapq.merge([1, 3, 5], [2, 4, 6])))",
        "print(list(heapq.merge([6, 4, 2], [5, 3, 1], reverse=True)))",
        "print(list(heapq.merge(['dog', 'horse'], ['cat', 'fish', 'kangaroo'], key=len)))",
        "for selection in (heapq.nsmallest, heapq.nlargest):",
        "    selection_calls = []",
        "    def selection_source():",
        "        for value in [3, 1, 2]:",
        "            selection_calls.append(value)",
        "            yield value",
        "    print(selection(0, selection_source()), selection_calls)",
        "    print(selection(-1, selection_source()), selection_calls)",
        "print(heapq.nsmallest(n=2, iterable=[3, 1, 2]), heapq.nlargest(n=2, iterable=[3, 1, 2]))",
        "consumed = []",
        "def sorted_source(label, values):",
        "    for value in values:",
        "        consumed.append(label + str(value))",
        "        yield value",
        "merged = heapq.merge(sorted_source('a', [1, 3]), sorted_source('b', [2, 4]))",
        "print(consumed); print(next(merged), consumed); print(next(merged), consumed)",
        "from contextlib import closing, contextmanager, nullcontext, suppress",
        "with suppress(KeyError): {'x': 1}['missing']",
        "with nullcontext(23) as value: print(value)",
        "events = []",
        "@contextmanager",
        "def managed():",
        "    events.append('enter')",
        "    try: yield 29",
        "    finally: events.append('exit')",
        "with managed() as value: events.append(value)",
        "print(events)",
      ].join("\n"),
    );
    assert.equal(
      result.stdout.trim(),
      [
        "[('a', 5), ('b', 2), ('r', 2)] 11 ['a', 'a', 'a', 'a', 'a', 'b', 'b', 'c', 'd', 'r', 'r']",
        "1 -1 0",
        "Counter({'a': 1, 'b': 2, 'c': 2})",
        "[('c', 3)] []",
        "most-common-float True 'float' object cannot be interpreted as an integer",
        "NotImplementedError Counter.fromkeys() is undefined.  Use Counter(iterable) instead.",
        "['a', 'a']",
        "elements-float True 'float' object cannot be interpreted as an integer",
        "True False False True False True",
        "False True True True False False",
        "False True True True False False",
        "True True",
        "Counter({'a': 2, 'b': -1}) Counter({'a': 9, 'b': -1})",
        "[True, True, True, True]",
        "True [('c', 3)]",
        "True [('c', 2)]",
        "True [('a', 1)]",
        "True [('a', 2), ('c', 3)]",
        "True [('a', 3), ('b', 3)]",
        "[1, 3] []",
        "True True [('odd', [1, 3, 5]), ('even', [])] [('odd', [1, 3, 5]), ('even', []), ('new', [7])] True True",
        "1 3 4 ['x', 'y', 'z']",
        "42 ['factory'] [('missing', 42)]",
        "('missing', 'absent') 17",
        "default [] []",
        "2 9 {'first': 1, 'missing': 9}",
        'chain-del-lower True ("Key not found in the first mapping: \'lower\'",) {\'lower\': 2}',
        "{'first': 1, 'missing': 9, 'mapped': 3, 'pair': 4, 'keyword': 5} {'lower': 2}",
        "1 fallback {'missing': 9, 'mapped': 3, 'pair': 4, 'keyword': 5} {'lower': 2}",
        'chain-pop-lower True ("Key not found in the first mapping: \'lower\'",)',
        "('last', 5) {'missing': 9, 'mapped': 3, 'pair': 4, 'keyword': 5} {'lower': 2}",
        "chain-popitem-empty True ('No keys found in the first mapping.',)",
        "{} {'lower': 2} 2",
        "False True FancyChainMap({'top': 1}, {'lower': 2, 'late': 7}) True False True",
        "True False True True",
        "True True",
        "[('b', 2), ('a', 1)]",
        "[('a', 1), ('b', 2)] ['b', 'a']",
        "move-missing True 'missing'",
        "pop-defaults True pop() takes at most 2 arguments (3 given) [('a', 1)]",
        "pop-defaults True pop() takes at most 2 arguments (3 given) []",
        "deque([0, 3, 1, 2], maxlen=4) 1 2",
        "deque([1, 2, 9, 0])",
        "2 2 2",
        "2",
        "deque(['c', 'd'])",
        "delete-range True deque index out of range",
        "True False True True True True",
        "[True, True, True, True, True, True]",
        "deque([1, 2, 3, 4]) deque([2, 3, 4], maxlen=3)",
        "deque([1, 2, 1, 2]) deque([1, 2, 1, 2]) deque([2, 1, 2], maxlen=3)",
        "True True",
        "True deque([2, 1, 2], maxlen=3)",
        "True deque([])",
        'add-list True can only concatenate deque (not "list") to deque',
        "repeat-float True 'float' object cannot be interpreted as an integer",
        "maxlen-float True an integer is required",
        "rotate-float True 'float' object cannot be interpreted as an integer",
        "index-float True slice indices must be integers or have an __index__ method",
        "maxlen-set True 3 [1, 2]",
        "maxlen-delete True 3 [1, 2]",
        "Point(x=2, y=5) 2 5",
        "6 None",
        "1024 True (2,)",
        "9 9 16 CacheInfo(hits=1, misses=2, maxsize=2, currsize=2)",
        "1 2 1 CacheInfo(hits=1, misses=2, maxsize=4, currsize=2)",
        "5 5 [5, 5] CacheInfo(hits=0, misses=2, maxsize=0, currsize=0) {'maxsize': 0, 'typed': False}",
        "maxsize-float True Expected first argument to be an integer, a callable, or None",
        "17 17 1",
        "['a', 'cc', 'bbb']",
        "True 17 23 wrapped True",
        "True {'local': 23, 'shared': 'wrapped', 'source': 17}",
        "True True True True",
        "True True True True",
        "True True True True",
        "True True True True",
        "ValueError must define at least one ordering operation: < > <= >=",
        "2.5 5",
        "0.6666666666666666 1",
        "4 1.7142857142857142",
        "2 0",
        "StatisticsError Weighted sum must be positive",
        "StatisticsError harmonic mean does not support negative values",
        "StatisticsError Number of weights does not match data size",
        "StatisticsError Weighted sum must be positive",
        "2 ['b']",
        "[2, 3, 4]",
        "[0.75, 1.5, 2.25]",
        "[0.4, 0.8, 1.2, 1.6, 2, 2.4, 2.8, 3.2, 3.6]",
        "[10, 10, 10]",
        "StatisticsError must have at least one data point",
        "2 1",
        "LinearRegression(slope=2, intercept=0)",
        "[1, 3, 4, 5] 1 2",
        "1 2",
        "1 1",
        "lo-float True 'float' object cannot be interpreted as an integer",
        "hi-float True argument should be integer or None, not 'float'",
        "lo-negative True lo must be non-negative",
        "[]",
        "[2]",
        "[1, 3, 4, 5]",
        "3 [1, 2]",
        "[1, 2, 3, 4, 5, 6]",
        "[6, 5, 4, 3, 2, 1]",
        "['dog', 'cat', 'fish', 'horse', 'kangaroo']",
        "[] []",
        "[] []",
        "[] []",
        "[] []",
        "[1, 2] [3, 2]",
        "[]",
        "1 ['a1', 'b2']",
        "2 ['a1', 'b2', 'a3']",
        "23",
        "['enter', 29, 'exit']",
      ].join("\n"),
    );
  } finally {
    await session.close();
  }
}

testUtilityModules()
  .then(() => console.log("Sage.js portable utility stdlib passed."))
  .catch((error) => {
    console.error(error);
    process.exitCode = 1;
  });
