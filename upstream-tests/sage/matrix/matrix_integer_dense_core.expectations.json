{
  "schema": "sagejs.sage-doctest-expectations/v1",
  "fixture": "matrix_integer_dense_core.doctests.json",
  "skip": {
    "src/sage/matrix/matrix_integer_dense.pyx:1357": "Sage.js exposes its own Matrix class rather than claiming Sage's Python module path",
    "src/sage/matrix/matrix_integer_dense.pyx:1363": "tests Sage's private algorithm-specific cache key rather than the public charpoly result",
    "src/sage/matrix/matrix_integer_dense.pyx:2464": "FLINT returns a valid but non-unique left Smith transformation; U*A*V == D is tested separately",
    "src/sage/matrix/matrix_integer_dense.pyx:2468": "FLINT returns a valid but non-unique right Smith transformation; U*A*V == D is tested separately",
    "src/sage/matrix/matrix_integer_dense.pyx:2485": "FLINT returns a valid but non-unique left Smith transformation; U*A*V == D is tested separately",
    "src/sage/matrix/matrix_integer_dense.pyx:2489": "FLINT returns a valid but non-unique right Smith transformation; U*A*V == D is tested separately",
    "src/sage/matrix/matrix_integer_dense.pyx:3904": "Sage.js intentionally represents exact small integers as JavaScript safe integers instead of boxed Sage Integer instances",
    "src/sage/matrix/matrix_integer_dense.pyx:5796": "Sage.js exposes its own Matrix class rather than claiming Sage's Python module path"
  },
  "xfail": {
    "src/sage/matrix/matrix_integer_dense.pyx:3861": "the deprecated LinBox selector computes the correct determinant but does not yet emit Sage's deprecation warning"
  }
}
